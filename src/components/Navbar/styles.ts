import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme: any) => ({
  menuIcon: {
    color: "black",
  },
  toolbar: {
    backgroundColor: "white",
  },
  box: {
    marginBottom: "2rem",
  },
}));
